name: Build OxyOS ISO

permissions:
  contents: write

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (e.g. 1.0)'
        required: true
        default: '1.0'
  push:
    tags:
      - 'v*'

jobs:
  build-shell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24.20.0

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Tauri system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential \
            clang \
            libayatana-appindicator3-dev \
            libclang-dev \
            libdbus-1-dev \
            libpam0g-dev \
            librsvg2-dev \
            libssl-dev \
            libwebkit2gtk-4.1-dev \
            libxdo-dev \
            patchelf \
            pkg-config

      - name: Cache Rust
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            shell/src-tauri/target
          key: rust-${{ hashFiles('shell/src-tauri/Cargo.lock') }}

      - name: Build shell frontend
        working-directory: shell
        run: |
          bun install --frozen-lockfile
          bun run test
          bun run lint
          bun run build:web

      - name: Build Tauri binary
        working-directory: shell
        run: bunx tauri build --bundles deb

      - name: Upload shell binary
        uses: actions/upload-artifact@v4
        with:
          name: oxyos-shell
          path: shell/src-tauri/target/release/oxyos-shell

  build-iso:
    runs-on: ubuntu-latest
    needs: build-shell
    container:
      image: debian:trixie
      options: --privileged
    steps:
      - name: Install dependencies
        run: |
          apt-get update
          apt-get install -y live-build git awscli

      - name: Checkout
        uses: actions/checkout@v4

      - name: Download shell binary
        uses: actions/download-artifact@v4
        with:
          name: oxyos-shell
          path: /tmp/shell-bin

      - name: Install shell binary into chroot
        run: |
          mkdir -p config/includes.chroot_after_packages/usr/bin
          cp /tmp/shell-bin/oxyos-shell config/includes.chroot_after_packages/usr/bin/oxyos-shell
          chmod +x config/includes.chroot_after_packages/usr/bin/oxyos-shell

      - name: Build ISO
        run: |
          mkdir -p .build && touch .build/config
          lb build

      - name: Prepare artifacts
        run: |
          ISO_FILE=$(ls *.iso 2>/dev/null | head -1)
          if [ -z "$ISO_FILE" ]; then
            echo "No ISO file found"
            exit 1
          fi

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            TAG_NAME="${GITHUB_REF_NAME}"
            VERSION="${TAG_NAME#v}"
          fi

          NEW_NAME="oxyos-${VERSION}-amd64.iso"
          mv "$ISO_FILE" "$NEW_NAME"
          echo "ISO_FILE=$NEW_NAME" >> $GITHUB_ENV
          echo "VERSION=$VERSION" >> $GITHUB_ENV

          md5sum "$NEW_NAME" | awk '{print $1}' > "${NEW_NAME}.md5"
          sha256sum "$NEW_NAME" | awk '{print $1}' > "${NEW_NAME}.sha256"
          echo "MD5_FILE=${NEW_NAME}.md5" >> $GITHUB_ENV
          echo "SHA256_FILE=${NEW_NAME}.sha256" >> $GITHUB_ENV

      - name: Upload to GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ${{ env.ISO_FILE }}
            ${{ env.MD5_FILE }}
            ${{ env.SHA256_FILE }}

      - name: Upload to DigitalOcean Spaces
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.DO_SPACES_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.DO_SPACES_SECRET_KEY }}
        run: |
          aws s3 cp "${{ env.ISO_FILE }}" "s3://oxyos/${{ env.ISO_FILE }}" \
            --endpoint-url https://ams3.digitaloceanspaces.com \
            --acl public-read
          aws s3 cp "${{ env.MD5_FILE }}" "s3://oxyos/${{ env.MD5_FILE }}" \
            --endpoint-url https://ams3.digitaloceanspaces.com \
            --acl public-read
          aws s3 cp "${{ env.SHA256_FILE }}" "s3://oxyos/${{ env.SHA256_FILE }}" \
            --endpoint-url https://ams3.digitaloceanspaces.com \
            --acl public-read
